{% extends 'base.html.twig' %}

{% block title %}Animal{% endblock %}

{% block body %}
    <h1>Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ animal.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('main_animal_index') }}">back to list</a>

    <a href="{{ path('main_animal_edit', {'id': animal.id}) }}">edit</a>

    {{ include('main/animal/_delete_form.html.twig') }}
{% endblock %}
